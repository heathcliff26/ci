---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: valkey-keepalived-renovate
  namespace: tekton-pipelines
spec:
  schedule: "30 * 12 * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: curl
            image: quay.io/curl/curl:8.20.0
            args: ["curl", "-X", "POST", "--data", '{"pipeline": "renovate", "repo": "valkey-keepalived"}', "el-cron.tekton-pipelines.svc.cluster.local:8080"]
          restartPolicy: Never
