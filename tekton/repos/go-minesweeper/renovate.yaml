---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: go-minesweeper-renovate
  namespace: tekton-ci
spec:
  schedule: "3 * 13 * *"
  timezone: "Europe/Berlin"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: curl
            image: quay.io/curl/curl:8.21.0
            args: ["curl", "-X", "POST", "--data", '{"pipeline": "renovate", "repo": "go-minesweeper"}', "el-cron.tekton-ci.svc.cluster.local:8080"]
          restartPolicy: Never
